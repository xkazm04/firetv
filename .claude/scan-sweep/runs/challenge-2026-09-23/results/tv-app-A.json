{"card":"tv-app A","status":"landed","shas":["f5ee209","75c1622","9dc5176"],"tests_added":8,
 "guards_declared":1,"cases_red_before":8,"cases_green_after":8,"cases_total":8,
 "gates":{".\\gradlew.bat :core:test :tv-app:compileDebugKotlin":0},"files_changed":5,"lines_changed":561,
 "deviations_from_card":"(1) Commit series follows the card's rollback (core+tests / host / doc), not test-then-feat: a tests-only commit would not compile, and the brief requires each commit green. (2) The GUARD case 8 could not be green before, because Heartbeat did not exist; all 8 were red by compilation. It is declared as a guard because it pins behaviour that already existed. (3) PairingDesk.open(openedAtMs, onUndecodable = {}) takes an optional hook so the host keeps its 'bad message' log line; onText still returns [] (case 6). (4) Session.kt gained heartbeatInput() (in the write set) so the host has a single read of the heartbeat's state. (5) The thumbnail is fetched at the beat's t rather than a second read of mediaTimeMs. Wire format and log strings unchanged. Doc citations per critic_revise: PLATFORM-RISK.md:71 (decision 3, dated addendum) and :54 (core row gains 'pen conversation').",
 "notes":"For tv-app B (marks on the state message): add `val marks: List<Long> = emptyList()` as the last field of HeartbeatInput (core/src/main/kotlin/dev/telestrator/core/PenConversation.kt), copy it in Heartbeat.beat's TvMessage.State(...) constructor (same file, `marks = input.marks`), and fill it in Session.heartbeatInput() (tv-app/.../Session.kt), e.g. `marks = AnnotationTimeline(doc.value).moments()`. PenSessionHost.kt needs no change for marks: it sends beat.state.copy(thumbnail = ...). HeartbeatTest case 8 compares the whole State with ==, so B's default emptyList() keeps it green. Test count in :core:test is now 54 (README.md:43 still says 46; B edits README). Not device-verified: pairing and heartbeat are unchanged on the wire, but live-ui-test.mjs / relay-test.mjs have not been run against the new host (no emulator here). Stale doc comment left alone, outside the write set: transport/PenTransport.kt:19 still says the conversation 'lives in' PenSessionHost."}
